game = [[2,0,1],
        [2,0, 0],
        [2,2,0],]

len(game)
colums = []
for col in colums
check = []

for row in game:
    print(row[0])
    check.append(row[0])

if check.count(check[0]) == len(check):
    print ("win")